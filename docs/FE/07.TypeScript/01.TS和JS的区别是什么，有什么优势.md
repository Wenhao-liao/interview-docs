# TS和JS的区别是什么，有什么优势

关系：

TS是JS的超集，TS比JS多了类型检查

一般的写服务端的语言都是强类型的语言，比如JAVA和C等

在编写阶段（不是运行阶段）就可以看到比如说函数参数的类型和返回值类型，并且输入的类型错误会进行提示

运行环境：

在JS运行在浏览器，和Nodejs的环境中

TS可以运行在Deno环境中，TS需要编译成JS才能够运行在浏览器和Nodejs环境中

编译：

TS需要经过编译才能运行在浏览器和node环境


优点：

1.编写或者编译的时候可以提前发现错误，不用运行的时候再发现，减少错误

2.文档有提示：变量，函数，参数和返回值
